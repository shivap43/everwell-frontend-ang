export enum PlanType {
    STANDARD = "STANDARD",
    REDIRECT = "REDIRECT",
    BUCKET = "BUCKET",
}

export enum ADVPlanType {
    EMPLOYER_PAID = "Employer-paid",
    EMPLOYEE_PAID = "Employee-paid",
}
