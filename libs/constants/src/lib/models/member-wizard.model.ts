export interface NextTab {
    label: string;
    link: string;
    backButtonLabel: string;
    nextButtonLabel: string;
}
export interface PolicyObject {
    newCost: number;
    description: string;
}
