// Captures the type of virtual call center.
// Applicable while creating a new toll-free number for a virtual call center.

export enum CallCenterType {
    INBOUND = "INBOUND",
    OUTBOUND = "OUTBOUND",
    BOTH = "BOTH",
}
