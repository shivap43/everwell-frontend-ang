export interface RSLIEligibility {
    rsliEligible: boolean; // a boolean of whether the minimum number of employees has been met
    employeesNeededForEligibility: number; // the number of employees needed for eligibility
}
