export enum SENT_UNSENT_LIST_COLUMNS {
    SELECT = "select",
    PRODUCER = "producerName",
    ENROLLMENT = "enrollment",
    NOTE = "note",
    COMMISSION_SPLIT = "commissionSplit",
    SEND_DATE = "sendDate",
}
