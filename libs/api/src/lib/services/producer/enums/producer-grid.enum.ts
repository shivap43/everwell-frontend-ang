export enum PRODUCER_GRID {
    NAME = "name",
    STATES = "states",
    NPN = "npn",
    SELECT = "select",
    MANAGE = "manage",
    ACCOUNT = "accountName",
    PRIMARY_PRODUCER = "primaryProducer",
}
