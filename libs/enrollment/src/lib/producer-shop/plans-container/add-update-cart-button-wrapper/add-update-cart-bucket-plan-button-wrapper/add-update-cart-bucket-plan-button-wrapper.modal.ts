import { AddUpdateCartButtonState } from "../add-update-cart-button-wrapper.model";

export interface AddUpdateCartButton {
    addUpdateCartButtonState: AddUpdateCartButtonState;
    isDisabled: boolean;
    toolTipText: string;
}
