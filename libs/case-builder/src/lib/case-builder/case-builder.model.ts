/**
 * Used for populating details of case builders in tabular form
 */
export interface CaseBuilderTableData {
    id: number;
    name: string;
    startDate: string;
    endDate: string;
    activateMenu: boolean;
    disableRemove: boolean;
}
